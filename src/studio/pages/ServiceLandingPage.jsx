import { Link } from "@tanstack/react-router";
import CallNowButton from "../components/CallNowButton";
export default function ServiceLandingPage({ service }) {
  const faqSchema = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: service.faq.map((item) => ({ "@type": "Question", name: item.question, acceptedAnswer: { "@type": "Answer", text: item.answer } })) };
  return <><section className="s28-page-hero"><div className="s28-container"><span className="s28-eyebrow">Studios 28 Services</span><h1>{service.h1}</h1><p>{service.intro}</p></div></section><section className="s28-section"><div className="s28-container" style={{ maxWidth: 860 }}>{service.copy.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}{/* TODO: Add approved Studios 28 service details and package information when available. */}<h2>Planning your {service.shortName.toLowerCase()} session</h2><p>{service.planning}</p><p>To see how different stories are approached, browse the <Link to="/portfolio">Studios 28 photography portfolio</Link>. When you are ready to discuss dates, location, and the kind of coverage you need, <Link to="/contact">contact our Bangalore photography studio</Link>. We will use that conversation to understand the brief before confirming availability or a tailored quote.</p><div style={{ marginTop: 24 }}><CallNowButton variant="gold" label="Discuss your shoot" /></div></div></section><section className="s28-section" style={{ background: "var(--s28-cream)" }}><div className="s28-container" style={{ maxWidth: 860 }}><span className="s28-eyebrow">FAQ</span><h2>{service.shortName} frequently asked questions</h2>{service.faq.map((item) => <div key={item.question}><h3>{item.question}</h3><p>{item.answer}</p></div>)}</div></section><script type="application/ld+json">{JSON.stringify(faqSchema)}</script></>;
}
